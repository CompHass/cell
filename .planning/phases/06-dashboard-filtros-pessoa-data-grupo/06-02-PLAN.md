---
phase: 06-dashboard-filtros-pessoa-data-grupo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/style.css
autonomous: true
requirements:
  - R004
must_haves:
  truths:
    - Dashboard header has date-from and date-to date inputs
    - Dashboard header has a person name text input
    - Group filter select already present and unchanged
    - Filter controls are visually grouped together in the header
  artifacts:
    - path: frontend/index.html
      provides: Filter input controls (date-from, date-to, person-name)
      contains: id="date-from"
    - path: frontend/style.css
      provides: Styling for new filter controls
  key_links:
    - from: frontend/app.js
      to: frontend/index.html
      via: document.getElementById
      pattern: "date-from|date-to|person-name"
---

<objective>
Add date range and person name filter controls to the dashboard HTML and style them consistently with the existing group filter.

Purpose: Provides UI elements for the date/person filters. app.js (Plan 03) will wire these to API calls.
Output: Updated `frontend/index.html` with 3 new inputs; updated `frontend/style.css` with filter styling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<interfaces>
<!-- Current HTML filter container — extend this -->

From frontend/index.html:
```html
<header>
    <h1>Cell Frequency Dashboard</h1>
    <div class="filter-container">
        <label for="group-filter">Group Filter:</label>
        <select id="group-filter">
            <option value="all">All Groups</option>
        </select>
    </div>
</header>
```

From frontend/style.css — existing filter-container class already present (read file for exact styles).

IDs that app.js will read (MUST match exactly):
- `date-from` — date input for start date
- `date-to` — date input for end date  
- `person-name` — text input for person filter
- `apply-filters` — button to trigger reload
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filter controls to HTML</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html (read full file — understand current structure before editing)
    - frontend/style.css (understand existing filter-container styling)
  </read_first>
  <action>
Inside the existing `<div class="filter-container">`, add the following controls AFTER the existing group-filter select:

```html
<label for="date-from">De:</label>
<input type="date" id="date-from">

<label for="date-to">Até:</label>
<input type="date" id="date-to">

<label for="person-name">Pessoa:</label>
<input type="text" id="person-name" placeholder="Nome...">

<button id="apply-filters">Filtrar</button>
```

Keep the existing group-filter select and its label intact. Do not change anything outside the filter-container div.
  </action>
  <verify>
    <automated>grep -c 'id="date-from"' /Users/hass/repos/github/comphass/cell_frequency/frontend/index.html</automated>
  </verify>
  <done>
    - `frontend/index.html` contains `id="date-from"`
    - `frontend/index.html` contains `id="date-to"`
    - `frontend/index.html` contains `id="person-name"`
    - `frontend/index.html` contains `id="apply-filters"`
    - `id="group-filter"` still present (unchanged)
  </done>
</task>

<task type="auto">
  <name>Task 2: Style the expanded filter bar</name>
  <files>frontend/style.css</files>
  <read_first>
    - frontend/style.css (read full file — match existing color/font/spacing conventions)
  </read_first>
  <action>
Read the existing `.filter-container` styles. Ensure the container uses `display: flex; flex-wrap: wrap; gap: 8px; align-items: center;` so inputs/labels wrap on small screens.

Add styles for the new inputs and button. Match the visual language of existing elements:
- `#date-from, #date-to` — `padding: 6px 10px; border: 1px solid #d1d5db; border-radius: 6px; font-size: 0.875rem;`
- `#person-name` — same as date inputs, `min-width: 140px;`
- `#apply-filters` — `padding: 6px 14px; background: #3b82f6; color: #fff; border: none; border-radius: 6px; cursor: pointer; font-size: 0.875rem;`
- `#apply-filters:hover` — `background: #2563eb;`

If `.filter-container` already has `display: flex`, update it rather than duplicate. Do not remove existing styles.
  </action>
  <verify>
    <automated>grep -c 'apply-filters' /Users/hass/repos/github/comphass/cell_frequency/frontend/style.css</automated>
  </verify>
  <done>
    - `frontend/style.css` contains `#apply-filters`
    - `frontend/style.css` contains `#person-name`
    - `frontend/style.css` contains `flex-wrap`
    - File is valid CSS (no parse errors visible in browser console)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→browser | Input fields accept free text and dates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | person-name text input | accept | Input passed as query param to local API; server-side ilike handles SQL injection via SQLAlchemy parameterization |
| T-06-05 | Info Disclosure | date inputs | accept | Local-only app; no external data exposure |
</threat_model>

<verification>
Open `http://localhost:8000` in browser. Verify:
1. Header shows: Group Filter select + De: date input + Até: date input + Pessoa: text input + Filtrar button
2. All controls are horizontally aligned (flex row)
3. Existing group filter select is unchanged
</verification>

<success_criteria>
HTML has 4 filter controls (group select + 2 date inputs + text input + button). CSS styles them consistently with existing header. No JS wired yet — that's Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-filtros-pessoa-data-grupo/06-02-SUMMARY.md`
</output>
