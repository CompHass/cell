---
phase: 06-dashboard-filtros-pessoa-data-grupo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
autonomous: true
requirements:
  - R007
  - R004
  - R005
must_haves:
  truths:
    - API endpoints accept date_from and date_to query params (ISO date strings)
    - API endpoints accept person_name query param (partial match, case-insensitive)
    - group_id filter continues to work unchanged
    - All 3 endpoints (/api/top-absent, /api/attendance-by-week, /api/top-present) support new params
  artifacts:
    - path: backend/main.py
      provides: Extended query params on all 3 endpoints
      contains: date_from
  key_links:
    - from: frontend/app.js
      to: backend/main.py
      via: fetch query string
      pattern: "date_from|date_to|person_name"
---

<objective>
Add `date_from`, `date_to`, and `person_name` query parameters to all 3 FastAPI attendance endpoints.

Purpose: Enables frontend to filter dashboard data by date range and individual person without reloading the page.
Output: Modified `backend/main.py` with extended query params on 3 endpoints.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current FastAPI endpoint signatures — extend these -->

From backend/main.py:
```python
@app.get("/api/top-absent")
def top_absent(group_id: str = Query(None), db: Session = Depends(get_db)):

@app.get("/api/attendance-by-week")
def attendance_by_week(group_id: str = Query(None), db: Session = Depends(get_db)):

@app.get("/api/top-present")
def top_present(group_id: str = Query(None), db: Session = Depends(get_db)):
```

Models in scope:
- `Person` — has `.name` (str), `.id`
- `Event` — has `.date` (datetime), `.id`
- `Attendance` — has `.person_id`, `.event_id`, `.group_id`, `.status` ("present"/"absent")

Existing import: `from fastapi import FastAPI, Depends, Query`
Existing import: `from datetime import datetime, timedelta`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add date and person filter params to all 3 endpoints</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py (read full file before editing — understand existing query patterns)
    - backend/models.py (confirm Person.name, Event.date column names)
  </read_first>
  <action>
Extend all 3 endpoint signatures with new optional query params. Add `date_from: str = Query(None)`, `date_to: str = Query(None)`, `person_name: str = Query(None)` to `top_absent`, `attendance_by_week`, and `top_present`.

For date filtering in each endpoint, after parsing: if `date_from` is provided, add `.filter(Event.date >= datetime.fromisoformat(date_from))`. If `date_to` is provided, add `.filter(Event.date <= datetime.fromisoformat(date_to))`.

For person_name filtering in `top_absent` and `top_present`: if `person_name` is provided, add `.filter(Person.name.ilike(f"%{person_name}%"))`.

For `attendance_by_week`: person_name filter joins through Person — add `.join(Person, Person.id == Attendance.person_id)` (if not already joined) then `.filter(Person.name.ilike(f"%{person_name}%"))` when person_name is set.

The existing `cutoff` date in `top_absent` (last 60 days) should be REPLACED by the date_from/date_to logic — when date_from or date_to is not provided, default to no cutoff (return all time). This makes the filter explicit and controllable from the UI.

Do NOT change return shapes, status codes, or existing group_id logic.
  </action>
  <verify>
    <automated>cd /Users/hass/repos/github/comphass/cell_frequency && python -c "from backend.main import app; print('import OK')"</automated>
  </verify>
  <done>
    - `backend/main.py` contains `date_from: str = Query(None)` (appears 3 times, once per endpoint)
    - `backend/main.py` contains `date_to: str = Query(None)` (appears 3 times)
    - `backend/main.py` contains `person_name: str = Query(None)` (appears 3 times)
    - `backend/main.py` contains `datetime.fromisoformat(date_from)` 
    - `backend/main.py` contains `.ilike(`
    - Python import succeeds without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→FastAPI | Filter params passed as query strings from local browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | date_from/date_to parsing | mitigate | Wrap `datetime.fromisoformat()` in try/except; return HTTP 422 on invalid format via FastAPI validation (use `date` type instead of `str` for auto-validation) |
| T-06-02 | Info Disclosure | person_name ilike | accept | Local-only app; no PII exposure risk beyond local network |
| T-06-03 | DoS | unbounded date range query | accept | Local app, single user; no rate limiting needed |
</threat_model>

<verification>
```bash
cd /Users/hass/repos/github/comphass/cell_frequency
python -c "from backend.main import app; print('import OK')"
grep -c "date_from" backend/main.py
grep -c "person_name" backend/main.py
```
Expected: import OK, date_from count >= 3, person_name count >= 3
</verification>

<success_criteria>
All 3 endpoints accept date_from, date_to, person_name query params. Python module imports clean. Manual curl `curl "http://localhost:8000/api/top-absent?date_from=2024-01-01&date_to=2024-12-31"` returns filtered JSON.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-filtros-pessoa-data-grupo/06-01-SUMMARY.md`
</output>
