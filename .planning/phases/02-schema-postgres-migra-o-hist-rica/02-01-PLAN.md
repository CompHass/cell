---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "backend/database.py"
  - "backend/models.py"
  - "scripts/requirements.txt"
autonomous: true
requirements: ["R002"]
must_haves:
  truths:
    - "SQLAlchemy models are defined and match the Postgres multi-grupo requirement."
  artifacts:
    - path: "backend/models.py"
      provides: "SQLAlchemy declarative base and models"
    - path: "backend/database.py"
      provides: "Engine and session maker"
  key_links:
    - from: "backend/models.py"
      to: "Postgres schema"
      via: "SQLAlchemy Base.metadata.create_all"
---

<objective>
Define the SQLAlchemy schema for the Postgres multi-grupo storage (R002).
Purpose: Establish the structural foundation for storing attendance and events, ensuring `group_id` is present on all models.
Output: SQLAlchemy models and database connection setup.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-schema-postgres-migra-o-hist-rica/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies to requirements</name>
  <read_first>scripts/requirements.txt</read_first>
  <action>Append `SQLAlchemy>=2.0.0` and `psycopg2-binary>=2.9.0` to `scripts/requirements.txt`.</action>
  <verify>
    <automated>grep -q 'SQLAlchemy' scripts/requirements.txt && grep -q 'psycopg2' scripts/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - scripts/requirements.txt contains `SQLAlchemy`
    - scripts/requirements.txt contains `psycopg2-binary`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Define SQLAlchemy Models</name>
  <read_first>backend/models.py</read_first>
  <action>Create `backend/models.py`. Define SQLAlchemy declarative Base.
Create 4 models with `__tablename__`:
- `groups`: `id` (String, PK), `name` (String, nullable)
- `persons`: `id` (String, PK), `name` (String), `group_id` (String, index=True)
- `events`: `id` (String, PK), `date` (DateTime), `name` (String), `group_id` (String, index=True)
- `attendance`: `event_id` (String, PK), `person_id` (String, PK), `status` (String), `group_id` (String, index=True)

Note: Make sure to include proper relationships if desired, but primarily ensure fields match the research.
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.append('.'); from backend.models import Base; print('Models OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/models.py contains `class Event(Base):`
    - backend/models.py contains `class Attendance(Base):`
    - group_id is present on all relevant tables
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Define Database Engine and Session</name>
  <read_first>backend/database.py</read_first>
  <action>Create `backend/database.py`. 
Set up SQLAlchemy `create_engine` using `DATABASE_URL` from environment (defaulting to `postgresql://postgres:postgres@localhost:5432/postgres` if not set).
Provide a `get_db()` function or `SessionLocal` sessionmaker. Include a `init_db()` function that calls `Base.metadata.create_all(bind=engine)`.
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.append('.'); from backend.database import get_db; print('DB OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/database.py contains `create_engine`
    - backend/database.py contains `SessionLocal = sessionmaker`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB Config | DB credentials passed via env vars |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Info Disclosure | DB | mitigate | Use environment variables for credentials |
</threat_model>
