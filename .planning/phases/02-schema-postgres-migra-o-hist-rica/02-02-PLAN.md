---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "scripts/migrate_history.py"
autonomous: true
requirements: ["R002", "R006"]
schema_push: true
must_haves:
  truths:
    - "Historical data can be idempotently inserted into Postgres."
  artifacts:
    - path: "scripts/migrate_history.py"
      provides: "Historical data loader"
  key_links:
    - from: "scripts/migrate_history.py"
      to: "Postgres schema"
      via: "SQLAlchemy session and upsert queries"
---

<objective>
Build the data migration script to load historical extracted JSON data into the Postgres database. (R006)
Purpose: Ensure we can reliably parse the historical JSON/ndjson artifacts and store them in the relational schema.
Output: Executable script `scripts/migrate_history.py`
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-schema-postgres-migra-o-hist-rica/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration script with Upserts</name>
  <read_first>backend/models.py, backend/database.py, scripts/migrate_history.py</read_first>
  <action>Create `scripts/migrate_history.py`.
It should:
1. Call `init_db()` from `backend.database` to ensure tables exist.
2. Read `artifacts/extract/attendance_rows.ndjson` and `artifacts/extract/event_summary.json`.
3. For idempotency, use SQLAlchemy's PostgreSQL `insert(...).on_conflict_do_update(...)` (from `sqlalchemy.dialects.postgresql`).
4. Iterate over `event_summary.json`, building `events` records. For the `group_id`, since `event_summary.json` might lack it, fetch the `group_id` from the corresponding event's attendance rows (read `attendance_rows.ndjson` first or concurrently).
5. Extract unique `persons` from `attendance_rows.ndjson` and upsert into `persons` table.
6. Extract `attendance` records from `attendance_rows.ndjson` and upsert into `attendance` table.
</action>
  <verify>
    <automated>python3 -c "import ast, sys; ast.parse(open('scripts/migrate_history.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/migrate_history.py uses `from sqlalchemy.dialects.postgresql import insert`
    - It reads `artifacts/extract/attendance_rows.ndjson`
    - It reads `artifacts/extract/event_summary.json`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Schema Verification / Push</name>
  <read_first>scripts/migrate_history.py</read_first>
  <action>No manual action needed here, this task represents the DB initialization and push which the script's `init_db()` will handle on startup. Verify the script runs and prints the expected success output (e.g. "Migration completed"). If DB is not available, handle gracefully or ensure Docker instruction is documented.</action>
  <verify>
    <automated>grep -q 'init_db()' scripts/migrate_history.py</automated>
  </verify>
  <acceptance_criteria>
    - `init_db()` is called inside the script.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File I/O | Reads local files and writes to local DB |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | DB | mitigate | Use ORM parameters to prevent injection |
</threat_model>
