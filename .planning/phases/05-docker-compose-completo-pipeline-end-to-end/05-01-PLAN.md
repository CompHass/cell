---
phase: 05-docker-compose-completo-pipeline-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - Dockerfile
  - requirements.txt
autonomous: true
requirements:
  - R007
must_haves:
  truths:
    - docker compose up starts the postgres and api services successfully
    - the API connects to the local postgres container
    - the frontend dashboard is accessible via the API service port
  artifacts:
    - path: docker-compose.yml
      provides: postgres and api services
    - path: Dockerfile
      provides: Unified Python environment for FastAPI and Playwright
  key_links:
    - from: api
      to: postgres
      via: DATABASE_URL environment variable
---

<objective>
Configurar o ambiente Docker Compose completo (Postgres + API/Dashboard) e o pipeline end-to-end.

Purpose: Rodar o sistema inteiro localmente sem dependências externas, unificando o runtime para rodar FastAPI e scripts de extração no mesmo container.
Output: docker-compose.yml e Dockerfile atualizados e funcionais.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify Python requirements</name>
  <files>requirements.txt, scripts/requirements.txt</files>
  <action>Create a unified `requirements.txt` at the project root combining FastAPI dependencies (fastapi, uvicorn, sqlalchemy, psycopg2-binary) and Playwright dependencies from `scripts/requirements.txt`. Remove `scripts/requirements.txt` if necessary or just symlink/include it.</action>
  <verify>
    <automated>cat requirements.txt | grep -c fastapi</automated>
  </verify>
  <done>requirements.txt contains all necessary packages for backend and scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Update Dockerfile for API</name>
  <files>Dockerfile</files>
  <action>Replace the existing Node/Playwright Dockerfile with a Python one. Base image: `mcr.microsoft.com/playwright/python:v1.41.0-jammy` (or similar) so playwright dependencies are present. Copy `requirements.txt` and `pip install -r requirements.txt`. Copy `backend/`, `scripts/`, and `frontend/`. Expose port 8000. Set command to `uvicorn backend.main:app --host 0.0.0.0 --port 8000`.</action>
  <verify>
    <automated>grep -c uvicorn Dockerfile</automated>
  </verify>
  <done>Dockerfile is configured to run the FastAPI app and includes Playwright.</done>
</task>

<task type="auto">
  <name>Task 3: Update docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>Replace the old `playwright-lab` service with two services: `postgres` (image: postgres:15, expose 5432, env POSTGRES_PASSWORD) and `api` (build from ., ports 8000:8000, env DATABASE_URL pointing to postgres). Set `api` to depend on `postgres`.</action>
  <verify>
    <automated>grep -c postgres docker-compose.yml</automated>
  </verify>
  <done>docker-compose.yml defines `postgres` and `api` services with proper linking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Local users accessing the dashboard |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Info Disclosure | API | accept | Local app; default local setup accepted |
</threat_model>

<verification>
Run `docker compose build && docker compose up -d`
Check if `curl -s http://localhost:8000/health` returns healthy.
Run `docker compose down`
</verification>

<success_criteria>
O `docker-compose.yml` sobe a stack inteira com sucesso e a API (servindo o frontend e endpoints) fica disponível e conectada ao Postgres.
</success_criteria>

<output>
After completion, create `.planning/phases/05-docker-compose-completo-pipeline-end-to-end/05-01-SUMMARY.md`
</output>
