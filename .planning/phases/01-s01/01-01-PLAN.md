---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/requirements.txt
  - scripts/extract.py
autonomous: true
requirements:
  - R001
user_setup:
  - service: "celula.in"
    why: "Credentials for extraction"
    env_vars:
      - name: "CELULA_EMAIL"
        source: "User login email"
      - name: "CELULA_PASSWORD"
        source: "User login password"

must_haves:
  truths:
    - "Script can authenticate via API"
    - "Script fetches group events"
    - "Script parses attendance and saves NDJSON/CSV"
  artifacts:
    - path: "scripts/extract.py"
      provides: "Python extraction logic"
      min_lines: 50
    - path: "scripts/requirements.txt"
      provides: "Python dependencies"
  key_links:
    - from: "scripts/extract.py"
      to: "celula.in/authenticate"
      via: "httpx.post"
---

<objective>
Implementar a extração de dados de frequência do celula.in em Python (portagem do script Node original).

Purpose: É a fonte primária de dados para todo o sistema (R001).
Output: `scripts/extract.py` e `scripts/requirements.txt`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-s01/01-CONTEXT.md
@.planning/phases/01-s01/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup dependencies</name>
  <files>scripts/requirements.txt</files>
  <read_first>
    - scripts/requirements.txt
    - .planning/phases/01-s01/01-RESEARCH.md
  </read_first>
  <action>
    Add `httpx` and `python-dotenv` to scripts/requirements.txt. These are the required libraries for HTTP requests and env var loading.
  </action>
  <verify>
    <automated>grep -q "httpx" scripts/requirements.txt && grep -q "python-dotenv" scripts/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - scripts/requirements.txt contains httpx
    - scripts/requirements.txt contains python-dotenv
  </acceptance_criteria>
  <done>Dependencies recorded</done>
</task>

<task type="auto">
  <name>Task 2: Implement extract.py</name>
  <files>scripts/extract.py</files>
  <read_first>
    - scripts/extract-attendance.mjs
    - .planning/phases/01-s01/01-RESEARCH.md
  </read_first>
  <action>
    Create or update `scripts/extract.py` as a line-by-line port of `extract-attendance.mjs` using `httpx.AsyncClient` instead of playwright.
    Must include:
    - `load_dotenv()`
    - `POST /authenticate` to get `access_token`
    - `GET /v1/groups/{id}?include=events`
    - `GET /v1/group-events/{id}?include=attendees,guests,group`
    - Writes to `artifacts/extract/attendance_rows.ndjson`, `event_summary.json`, `group_events_raw.ndjson`, `attendance.csv`, `attendance_summary_by_person.csv`, and `group.json`.
    Make sure to match the same output schema exactly.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/extract.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/extract.py contains def authenticate(
    - scripts/extract.py contains httpx.AsyncClient
    - script has valid python syntax (ast.parse passes)
  </acceptance_criteria>
  <done>Script implemented and syntactically valid</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> celula.in API | Credentials sent to 3rd party API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Info disclosure | extract.py | mitigate | Load credentials from .env via dotenv, do not hardcode |
| T-01-02 | Spoofing | celula.in API | accept | We trust the celula.in endpoint over HTTPS |
</threat_model>

<verification>
python3 -c "import ast; ast.parse(open('scripts/extract.py').read())"
</verification>

<success_criteria>
- extract.py is created and valid python
- Requirements.txt contains httpx and python-dotenv
</success_criteria>

<output>
After completion, create `.planning/phases/01-s01/01-01-SUMMARY.md`
</output>
