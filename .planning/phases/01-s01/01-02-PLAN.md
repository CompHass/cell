---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/verify_extract.py
autonomous: true
requirements:
  - R001
must_haves:
  truths:
    - "Schema matches the Node.js implementation"
  artifacts:
    - path: "scripts/verify_extract.py"
      provides: "Automated schema verification"
      min_lines: 20
  key_links:
    - from: "scripts/verify_extract.py"
      to: "artifacts/extract/attendance_rows.ndjson"
      via: "json.loads"
---

<objective>
Validar a saída do script Python (schema verification) contra os artefatos antigos.

Purpose: Garantir que a portagem para Python não introduziu regressões no formato dos dados (R001).
Output: `scripts/verify_extract.py` que testa se `attendance_rows.ndjson` contém as colunas certas.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-s01/01-CONTEXT.md
@.planning/phases/01-s01/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verification script</name>
  <files>scripts/verify_extract.py</files>
  <read_first>
    - scripts/verify_extract.py
    - .planning/phases/01-s01/01-RESEARCH.md
  </read_first>
  <action>
    Create `scripts/verify_extract.py` that implements the verification from the RESEARCH document.
    Must read `artifacts/extract/attendance_rows.ndjson` line-by-line and assert that all lines have the required keys:
    `{'event_id', 'event_date', 'event_name', 'person_id', 'person_name', 'status', 'group_id'}`
    Also verify `artifacts/extract/event_summary.json` has `event_id` and `attendees_count`.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/verify_extract.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify_extract.py asserts on attendance_rows.ndjson keys
    - script has valid python syntax
  </acceptance_criteria>
  <done>Verification script implemented and syntactically valid</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system | Reads JSON objects from local artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | verification script | accept | Local execution only, low risk |
</threat_model>

<verification>
python3 -c "import ast; ast.parse(open('scripts/verify_extract.py').read())"
</verification>

<success_criteria>
- verify_extract.py created and syntactically correct
</success_criteria>

<output>
After completion, create `.planning/phases/01-s01/01-02-SUMMARY.md`
</output>
