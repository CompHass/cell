---
phase: 03-api-fastapi-com-endpoints-de-frequ-ncia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["scripts/requirements.txt", "backend/main.py"]
autonomous: true
requirements: [R003, R005]

must_haves:
  truths:
    - "FastAPI server runs without syntax errors"
    - "API documentation is accessible at /docs"
    - "Available groups can be listed via API"
  artifacts:
    - path: "backend/main.py"
      provides: "FastAPI application and endpoints"
    - path: "scripts/requirements.txt"
      provides: "fastapi and uvicorn dependencies"
  key_links:
    - from: "backend/main.py"
      to: "backend/database.py"
      via: "SQLAlchemy Session dependency"
---

<objective>
Initialize the FastAPI server, configure CORS, and create the core group-listing endpoint.
Purpose: Establish the REST API foundation to serve data to the dashboard.
Output: Working FastAPI application with Swagger documentation and the `/api/groups` endpoint.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/models.py
</context>

<interfaces>
From backend/database.py:
```python
def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```
From backend/models.py:
```python
class Group(Base):
    __tablename__ = "groups"
    id = Column(String, primary_key=True)
    name = Column(String, nullable=True)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add API dependencies</name>
  <files>scripts/requirements.txt</files>
  <read_first>scripts/requirements.txt</read_first>
  <action>Append `fastapi` and `uvicorn` to `scripts/requirements.txt`.</action>
  <verify>
    <automated>grep "fastapi" scripts/requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/requirements.txt` contains `fastapi`
    - `scripts/requirements.txt` contains `uvicorn`
  </acceptance_criteria>
  <done>Dependencies added to requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize FastAPI application</name>
  <files>backend/main.py</files>
  <read_first>backend/database.py, backend/models.py</read_first>
  <action>Create a FastAPI app with CORS middleware (allow all origins). Implement a root endpoint `GET /` returning `{"status": "ok"}` and a healthcheck endpoint. Ensure you import `get_db` from `backend.database`.</action>
  <verify>
    <automated>grep -c "FastAPI()" backend/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/main.py` contains `app = FastAPI()`
    - `backend/main.py` configures `CORSMiddleware` with `allow_origins=["*"]`
    - `backend/main.py` contains a `GET /` endpoint returning `{"status": "ok"}`
  </acceptance_criteria>
  <done>FastAPI application defined with CORS.</done>
</task>

<task type="auto">
  <name>Task 3: Implement /api/groups endpoint</name>
  <files>backend/main.py</files>
  <read_first>backend/main.py, backend/models.py</read_first>
  <action>Implement `GET /api/groups` endpoint returning all available groups from the DB. Use the `Depends(get_db)` dependency to get the DB session. Query all `Group` records from `backend.models`.</action>
  <verify>
    <automated>grep -c "@app.get(\"/api/groups\")" backend/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/main.py` contains `@app.get("/api/groups")`
    - Endpoint uses `db: Session = Depends(get_db)`
    - Endpoint queries the `Group` model and returns the list of groups
  </acceptance_criteria>
  <done>Group list endpoint implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Any HTTP request hitting the API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `/api/groups` | accept | Read-only endpoint; no tampering possible as it only lists public groups. |
</threat_model>

<verification>
- Verify dependencies are written correctly.
- Verify FastAPI initializes without errors.
</verification>

<success_criteria>
- `backend/main.py` exists and has `FastAPI()`.
- `/api/groups` is defined.
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-fastapi-com-endpoints-de-frequ-ncia/03-01-SUMMARY.md`
</output>