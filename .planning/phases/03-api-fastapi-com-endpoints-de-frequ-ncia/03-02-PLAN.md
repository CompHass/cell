---
phase: 03-api-fastapi-com-endpoints-de-frequ-ncia
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["backend/main.py"]
autonomous: true
requirements: [R003, R004, R005]

must_haves:
  truths:
    - "Analytical endpoints are accessible via API"
    - "Every stats query requires a group_id parameter"
    - "No group data leak occurs"
  artifacts:
    - path: "backend/main.py"
      provides: "Stats endpoints for the dashboard"
  key_links:
    - from: "backend/main.py"
      to: "backend/models.py"
      via: "SQLAlchemy analytical queries"
---

<objective>
Implement analytical endpoints to support the dashboard charts.
Purpose: Provide computed data for (1) Top Ausentes, (2) Top Presentes, and (3) Attendance by week.
Output: Three new endpoints filtering securely by `group_id`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/models.py
</context>

<interfaces>
From backend/models.py:
```python
class Person(Base): ...
class Event(Base): ...
class Attendance(Base): ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement Top Absent Endpoint</name>
  <files>backend/main.py</files>
  <read_first>backend/main.py, backend/models.py</read_first>
  <action>Implement `GET /api/stats/top-absent`. Accept mandatory query param `group_id: str`. Filter `Attendance` by `group_id` AND `status == 'absent'` (over the last 2 months if possible, or all-time). Return the top people with highest absence counts. Join with `Person` to return names.</action>
  <verify>
    <automated>grep -c "@app.get(\"/api/stats/top-absent\")" backend/main.py</automated>
  </verify>
  <acceptance_criteria>
    - Endpoint requires `group_id`
    - Queries `Attendance` where `status == 'absent'`
    - Appends `.filter(Attendance.group_id == group_id)`
  </acceptance_criteria>
  <done>Top absent endpoint implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Top Present Endpoint</name>
  <files>backend/main.py</files>
  <read_first>backend/main.py, backend/models.py</read_first>
  <action>Implement `GET /api/stats/top-present`. Accept mandatory query param `group_id: str`. Filter `Attendance` by `group_id` AND `status == 'present'`. Return the top people with highest presence counts. Join with `Person` to return names.</action>
  <verify>
    <automated>grep -c "@app.get(\"/api/stats/top-present\")" backend/main.py</automated>
  </verify>
  <acceptance_criteria>
    - Endpoint requires `group_id`
    - Queries `Attendance` where `status == 'present'`
    - Appends `.filter(Attendance.group_id == group_id)`
  </acceptance_criteria>
  <done>Top present endpoint implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Attendance by Week Endpoint</name>
  <files>backend/main.py</files>
  <read_first>backend/main.py, backend/models.py</read_first>
  <action>Implement `GET /api/stats/attendance-by-week`. Accept mandatory query param `group_id: str`. Query `Attendance` joined with `Event`, grouped by `Event.date` or event name/id. Return a summary of present vs absent counts per event for the given group.</action>
  <verify>
    <automated>grep -c "@app.get(\"/api/stats/attendance-by-week\")" backend/main.py</automated>
  </verify>
  <acceptance_criteria>
    - Endpoint requires `group_id`
    - Queries `Attendance` grouped by `event_id` or week
    - Appends `.filter(Attendance.group_id == group_id)`
  </acceptance_criteria>
  <done>Weekly attendance summary implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Any HTTP request hitting the API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Info Disclosure | `backend/main.py` | mitigate | Mandatory `group_id` parameter and forced SQLAlchemy `.filter()` |
</threat_model>

<verification>
- Verify all endpoints enforce `group_id`.
- Verify analytical queries use `func.count` or return correct aggregations.
</verification>

<success_criteria>
- Three stats endpoints available under `/api/stats/`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-fastapi-com-endpoints-de-frequ-ncia/03-02-SUMMARY.md`
</output>