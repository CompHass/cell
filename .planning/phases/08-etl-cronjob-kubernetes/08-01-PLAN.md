---
phase: 08-etl-cronjob-kubernetes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/secret.yaml
autonomous: true
requirements: []

must_haves:
  truths:
    - "k8s/secret.yaml contém DATABASE_URL, CELULA_EMAIL, CELULA_PASSWORD, CELULA_ACCOUNT, CELULA_GROUP_ID"
    - "Nenhum valor de secret hardcoded — placeholders claros para substituição antes do apply"
    - "Estrutura do Secret mantida (stringData, namespace cell)"
  artifacts:
    - path: "k8s/secret.yaml"
      provides: "Secret completo com todas as credenciais do ETL pipeline"
---

<objective>
Adicionar credenciais do pipeline ETL ao Secret Kubernetes existente.

O `k8s/secret.yaml` atualmente contém apenas `DATABASE_URL`. Precisamos adicionar as variáveis de ambiente necessárias para o `extract.py` rodar dentro do cluster:
- `CELULA_EMAIL`
- `CELULA_PASSWORD`
- `CELULA_ACCOUNT`
- `CELULA_GROUP_ID`

Output: `k8s/secret.yaml` atualizado com todos os campos, valores com placeholders documentados.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

Estado atual de k8s/secret.yaml:
```yaml
apiVersion: v1
kind: Secret
metadata:
  name: cell-secret
  namespace: cell
type: Opaque
stringData:
  DATABASE_URL: "postgresql://postgres:password@postgres:5432/postgres"
```

Vars necessárias para extract.py:
- `CELULA_EMAIL` — email de login na API celula.in
- `CELULA_PASSWORD` — senha de login
- `CELULA_ACCOUNT` — account slug (ex: "edificando")
- `CELULA_GROUP_ID` — IDs de grupos separados por vírgula (ex: "123,456")
- `DATABASE_URL` — já existe, manter

IMPORTANTE: k8s/secret.yaml NÃO deve conter valores reais (é versionado no git).
Usar placeholders com comentários explicativos.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar k8s/secret.yaml com credenciais ETL</name>
  <files>k8s/secret.yaml</files>
  <action>
Substituir o conteúdo de k8s/secret.yaml com o Secret completo incluindo todas as credenciais do ETL.

Usar placeholders descritivos para os valores (o arquivo é versionado — valores reais ficam fora do git):

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: cell-secret
  namespace: cell
type: Opaque
stringData:
  # Database connection
  DATABASE_URL: "postgresql://postgres:password@postgres:5432/postgres"

  # celula.in API credentials (required for ETL CronJob)
  # Replace with real values before applying: kubectl apply -f k8s/secret.yaml
  CELULA_EMAIL: "your-email@example.com"
  CELULA_PASSWORD: "your-password"
  CELULA_ACCOUNT: "your-account-slug"

  # Comma-separated group IDs to extract (e.g. "123,456")
  CELULA_GROUP_ID: "your-group-id"
```

NOTA: O valor atual de DATABASE_URL pode ser um placeholder — não é o valor real de produção. Manter como está.
  </action>
  <verify>
    grep -q "CELULA_EMAIL" k8s/secret.yaml && echo "CELULA_EMAIL OK"
    grep -q "CELULA_PASSWORD" k8s/secret.yaml && echo "CELULA_PASSWORD OK"
    grep -q "CELULA_ACCOUNT" k8s/secret.yaml && echo "CELULA_ACCOUNT OK"
    grep -q "CELULA_GROUP_ID" k8s/secret.yaml && echo "CELULA_GROUP_ID OK"
    grep -q "DATABASE_URL" k8s/secret.yaml && echo "DATABASE_URL OK"
  </verify>
  <done>k8s/secret.yaml contém todas as 5 keys com placeholders e comentários</done>
</task>

</tasks>

<verification>
```bash
grep -c "CELULA_" k8s/secret.yaml  # deve retornar 4
grep "name: cell-secret" k8s/secret.yaml && echo "Secret name OK"
grep "namespace: cell" k8s/secret.yaml && echo "Namespace OK"
```
</verification>

<success_criteria>
- k8s/secret.yaml atualizado com DATABASE_URL + 4 vars CELULA_*
- Placeholders claros, sem valores reais versionados
- Comentários explicando como aplicar com valores reais
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-etl-cronjob-kubernetes/08-01-SUMMARY.md`
</output>
