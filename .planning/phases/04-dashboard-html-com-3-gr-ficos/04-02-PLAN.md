---
phase: 04-dashboard-html-com-3-gr-ficos
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/app.js
  - frontend/index.html
autonomous: true
requirements:
  - R004
  - R005
must_haves:
  truths:
    - User can filter charts by group_id
    - Charts render correctly based on API data
  artifacts:
    - path: frontend/app.js
      provides: Chart rendering and API fetching
      min_lines: 50
  key_links:
    - from: frontend/app.js
      to: backend/main.py
      via: fetch
      pattern: /api/stats/
---

<objective>
Implement frontend logic to fetch API data and render the 3 priority charts.
Purpose: Complete the dashboard presentation by rendering the analytical data.
Output: Working charts with group filtering capability.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch groups and populate filter</name>
  <read_first>
    - frontend/index.html
    - backend/main.py
  </read_first>
  <action>
    Create `frontend/app.js`. Fetch groups from `/api/groups` and populate the `<select id="group-filter">`. When the selection changes, trigger a function to update all charts, passing the selected `group_id`. Link `app.js` in `frontend/index.html`.
  </action>
  <verify>
    <automated>grep -c "/api/groups" frontend/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `app.js` makes `fetch('/api/groups')`
    - `index.html` includes `<script src="app.js"></script>`
  </acceptance_criteria>
  <done>Group filter is populated and interactive.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Chart rendering</name>
  <read_first>
    - frontend/app.js
    - backend/main.py
  </read_first>
  <action>
    In `frontend/app.js`, implement data fetching for `/api/stats/top-absent?group_id=...`, `/api/stats/attendance-by-week?group_id=...`, and `/api/stats/top-present?group_id=...`. Use Chart.js to render these into the respective canvas elements. Destroy and recreate or update Chart instances on group change.
  </action>
  <verify>
    <automated>grep -c "/api/stats/top-absent" frontend/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `app.js` fetches from `/api/stats/top-absent`
    - `app.js` fetches from `/api/stats/attendance-by-week`
    - `app.js` fetches from `/api/stats/top-present`
    - Chart instances are managed (created/updated) correctly
  </acceptance_criteria>
  <done>Three priority charts render dynamic data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Frontend requests to API endpoints |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Info disclosure | Frontend App | mitigate | API filters data strictly by group_id |
</threat_model>
