---
phase: 04-dashboard-html-com-3-gr-ficos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - frontend/index.html
  - frontend/style.css
autonomous: true
requirements:
  - R004
  - R005
must_haves:
  truths:
    - User can load the dashboard via FastAPI root
    - Dashboard layout is visible
  artifacts:
    - path: frontend/index.html
      provides: Dashboard skeleton
      min_lines: 20
    - path: frontend/style.css
      provides: Dashboard styling
      min_lines: 20
  key_links:
    - from: backend/main.py
      to: frontend/index.html
      via: static file mounting
      pattern: StaticFiles
---

<objective>
Setup dashboard HTML skeleton and serve via FastAPI.
Purpose: Create the presentation layer for the analytics charts.
Output: Mounted frontend directory and base HTML/CSS.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount static frontend in FastAPI</name>
  <read_first>
    - backend/main.py
  </read_first>
  <action>
    Create a `frontend` directory. Update `backend/main.py` to mount the `frontend` directory as static files at `/`. Ensure the root path `/` serves `frontend/index.html` (e.g., using `FileResponse("frontend/index.html")` or `StaticFiles(html=True)`). Remove or update the existing `@app.get("/")` JSON response if needed.
  </action>
  <verify>
    <automated>grep -c "StaticFiles" backend/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/main.py` imports `StaticFiles` from `fastapi.staticfiles`
    - `app.mount("/", StaticFiles(directory="frontend", html=True), name="frontend")` or equivalent is configured
  </acceptance_criteria>
  <done>Frontend static files mapped to root.</done>
</task>

<task type="auto">
  <name>Task 2: Create base HTML layout</name>
  <read_first>
    - backend/main.py
  </read_first>
  <action>
    Create `frontend/index.html` with a base layout containing a header, a group filter `<select id="group-filter">`, and 3 `<canvas>` elements for the charts: `topAbsentChart`, `attendanceByWeekChart`, and `topPresentChart`. Include Chart.js via CDN `<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>`. Create `frontend/style.css` for basic styling (Open Design principles: simple, clean, card-based).
  </action>
  <verify>
    <automated>grep -c "chart.js" frontend/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/index.html` contains `<canvas id="topAbsentChart">`
    - `frontend/index.html` contains Chart.js CDN script
    - `frontend/index.html` links to `style.css`
  </acceptance_criteria>
  <done>Dashboard HTML skeleton created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Static files are served directly |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | StaticFiles | accept | Static files are local and read-only |
</threat_model>
